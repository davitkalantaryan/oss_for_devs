# the location of this file is '/home/ers/bin'
as_root() {   if   [ $EUID = 0 ];        then $*;   elif [ -x /usr/bin/sudo ]; then sudo $*;   else                            su -c \\"$*\\";   fi; }
export -f as_root
export PATH=/home/ers/bin:${PATH}
